# ImageView 文件拖入打开设计

日期：2026-07-10
状态：已确认，待实现

## 行为

窗口接受 Finder 拖入的第一个文件 URL。支持格式时沿用现有 `open(url:)` 路径，因此目录扫描、预加载和未保存编辑确认行为完全一致；不支持格式沿用现有错误提示。拖入期间画布显示轻量高亮边框。

## 实现

根视图注册 `.fileURL` 拖放类型，`performDragOperation` 将首个 URL 交给控制器回调。控制器不直接绕过 `open(url:)`，因此不会引入第二套文件状态逻辑。测试覆盖接受的拖放类型与首个 URL 选择。
